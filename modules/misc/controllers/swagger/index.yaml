swagger: "2.0"
info:
  version: "12"
  title: The clickyab.com/crab API
  description: The crab api, auto generated
host: 127.0.0.1
basePath: /api
schemes:
- http
consumes:
- application/json
produces:
- application/json
paths:
  /user/dummy:
    get:
      description: dummy route. Remove it when the real routes are ready
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/controller_NormalResponse'
        "400":
          description: ""
          schema:
            $ref: '#/definitions/controller_ErrorResponseSimple'
      tags:
      - user
  /user/mail/check:
    post:
      description: checkMail check mail in system
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/user_checkMailResponse'
        "404":
          description: ""
          schema:
            $ref: '#/definitions/controller_ErrorResponseSimple'
      parameters:
      - name: payload_data
        schema:
          $ref: '#/definitions/user_checkMailPayload'
        description: ""
        in: body
        required: false
      tags:
      - user
definitions:
  controller_ErrorResponseSimple:
    properties:
      error:
        properties:
          params:
            items:
              type: string
            type: array
          text:
            type: string
        type: object
    type: object
  controller_NormalResponse:
    properties: {}
    type: object
  dmn_Domain:
    properties:
      active:
        type: integer
      created_at:
        format: dateTime
        type: string
      description:
        properties:
          String:
            type: string
          Valid:
            type: boolean
        type: object
      id:
        type: integer
      name:
        type: string
      updated_at:
        format: dateTime
        type: string
    type: object
  trans_t9Base:
    properties:
      params:
        items:
          type: string
        type: array
      text:
        type: string
    type: object
  user_checkMailPayload:
    properties:
      email:
        type: string
    type: object
  user_checkMailResponse:
    properties:
      current_domain:
        type: boolean
      domains:
        items:
          properties:
            active:
              type: integer
            created_at:
              format: dateTime
              type: string
            description:
              properties:
                String:
                  type: string
                Valid:
                  type: boolean
              type: object
            id:
              type: integer
            name:
              type: string
            updated_at:
              format: dateTime
              type: string
          type: object
        type: array
    type: object
